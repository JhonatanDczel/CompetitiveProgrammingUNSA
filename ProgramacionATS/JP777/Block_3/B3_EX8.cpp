#include <iostream>
using namespace std;
int main(){
  int a; cin>>a;
  int b; cin>>b;
  int c; cin>>c;
  int d; cin>>d;
  if(d == a || d == b || d == c){
    cout<<"Si coincide";
  }
  else{
    cout<<"No coincide";
  }
}

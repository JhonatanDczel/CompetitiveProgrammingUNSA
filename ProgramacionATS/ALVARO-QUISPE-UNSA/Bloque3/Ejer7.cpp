#include <iostream> 
using namespace std;
int main() {
    int a;
    cin>>a;
    if (18 <= a && a <= 20) cout<<"Está en el rango"<<endl;
    else cout<<"No está en el rango"<<endl;
    system("pause");
    return 0;
}